using UnityEngine;

public class CameraService: Service
{
       // TODO
       /*
       private readonly GameEntity _player;

       public Camera LoadCamera()
       {

              if (target != null)
              {
                     SetUpCameraEntity(_contexts.game.CreateEntity());
              }
              var camera = 
              camera.isCamera = true;
              camera.AddId(id);
              camera.AddAsset("MainCamera");
              camera.AddPosition();
              entity.AddVelocity(_contexts.config.initialVelocity.value);
       }

       private void SetUpCameraEntity(GameEntity cameraEntity)
       {
              cameraEntity.isCamera = true;
       }

       public CameraSetTarget()
       {
              
       }
       
       public CameraService(Contexts contexts) : base(contexts)
       { 
              _player = _contexts.game.playerEntity;
       }
       */
       
       public CameraService(Contexts contexts) : base(contexts)
       {
       }
}